# Claude Mate: Dead Code
# Runs on merge to main and nightly schedule

name: "Mate: Dead Code"

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1-5'  # Weekdays 6am UTC
  workflow_dispatch: {}

concurrency:
  group: mate-dead-code
  cancel-in-progress: false


permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  dead-code:
    uses: ./.github/workflows/_run-mate.yml
    with:
      mate: "dead-code"
      mates_root: "."
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_MATES_API_KEY }}
